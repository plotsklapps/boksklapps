import 'package:boksklapps/all_imports.dart';

final StateProvider<List<int>> punchListProvider = StateProvider<List<int>>(
  (StateProviderRef<List<int>> ref) {
    return <int>[];
  },
);

final StateProvider<bool> punch3Provider = StateProvider<bool>(
  (StateProviderRef<bool> ref) {
    return false;
  },
);

final StateProvider<bool> punch1Provider = StateProvider<bool>(
  (StateProviderRef<bool> ref) {
    return false;
  },
);

final StateProvider<bool> punch2Provider = StateProvider<bool>(
  (StateProviderRef<bool> ref) {
    return false;
  },
);

final StateProvider<bool> punch4Provider = StateProvider<bool>(
  (StateProviderRef<bool> ref) {
    return false;
  },
);

final StateProvider<bool> punch5Provider = StateProvider<bool>(
  (StateProviderRef<bool> ref) {
    return false;
  },
);

final StateProvider<bool> punch6Provider = StateProvider<bool>(
  (StateProviderRef<bool> ref) {
    return false;
  },
);

final StateProvider<bool> punch7Provider = StateProvider<bool>(
  (StateProviderRef<bool> ref) {
    return false;
  },
);

final StateProvider<bool> punch8Provider = StateProvider<bool>(
  (StateProviderRef<bool> ref) {
    return false;
  },
);
