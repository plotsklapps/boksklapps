name: boksklapps
description: Boxing Cardio App

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
  cloud_firestore: ^4.3.1
  firebase_analytics: ^10.1.0
  firebase_auth: ^4.2.5
  firebase_core: ^2.4.1
  flex_color_scheme: ^7.0.0-dev.2
  flutter:
    sdk: flutter
  flutter_animate: ^3.0.0
  flutter_riverpod: ^2.1.3
  google_fonts: ^4.0.1

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  very_good_analysis: ^3.1.0

flutter:
  uses-material-design: true
  assets:
    - assets
